/** @type {import('next').NextConfig} */
const nextConfig = {
  // 👇 This tells Next.js to ignore all TypeScript build errors
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
